//TODO:
// setup standard for types that represent api calls.
// process the WakatimePulse into a CodeClimbers.Pulse and save it to the database
const createPulse = async (pulse: CodeClimbersApi.CreateWakatimePulseDto) => {
  // process the pulse
  // const pulseToSave: CodeClimbers.Pulse = {
  //   userId: pulse.userId,
  //   project: pulse.project,
  //   branch: pulse.branch,
  // }
}

export default {
  createPulse,
}
